# 사용법 가이드 캡처

사이트 **사용법 가이드**(`/guide`, 파트너용)에 들어가는 화면 캡처입니다.
이미 캡처가 들어가 있습니다 — 그대로 쓰면 됩니다.

- `p1.png` — 내 작업실 (오늘 상태·연속일·글 제출)
- `p2.png` — 자료실 (사진·영상 다운로드)
- `p3.png` — 전체 인증 보드

## 다시 찍고 싶을 때 (선택)
화면이 바뀌어 캡처를 새로 넣고 싶으면, 위 파일명 그대로 이 폴더에 덮어쓰면 됩니다.
(직접 캡처: 브라우저에서 해당 페이지 → `Win + Shift + S`)
